# School-specific overlays (contain real PII)
config-*-overlay.yaml
!config-*-overlay.example.yaml

# Work logs (contain local paths)
WORKLOG.md

# Draft schema versions
config-schema-v2-a.yaml
config-schema-v2-b.yaml
config-schema-v2-merged.yaml
config-schema.yaml

# OS files
.DS_Store
*.swp
*~
